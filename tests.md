# Tests

## Test 1: Full session run (Issue 2)

Run training on GPU from phone, let the session run normally. Confirm training completes
and plots appear on GitHub on schedule.

## Test 2: Deliberate disconnect (Issue 2)

Run training on GPU from phone, deliberately disconnect the session mid-run. Confirm
training keeps going and plots still push to GitHub on schedule. This validates Hack 1.
