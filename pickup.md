# Pickup

## Next session

1. Reboot the laptop (required — GPU has fatal Xid 31 / UVM error 0x60)
2. `sudo modprobe nvidia_uvm`
3. Verify CUDA is healthy
4. Disable auto-suspend (see `gpu_protocol.md`)
5. Fresh training runs from phone
